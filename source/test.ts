import { equal } from 'assert-helpers'
import kava from 'kava'
import fne from './index.js'

kava.suite('facebook-export-notes', function (suite, test) {
	test('no tests yet', function () {
		console.log('no tests yet')
	})
})
